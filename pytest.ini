[pytest]
# Only search for tests in the tests/ directory
testpaths = tests
pythonpath = .

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Timeout for each test (prevents hanging)
timeout = 30

# Show extra test summary info
addopts = -v --tb=short
